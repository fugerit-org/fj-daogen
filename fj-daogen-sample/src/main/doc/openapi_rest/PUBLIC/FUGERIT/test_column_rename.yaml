openapi: 3.0.0
info:
  title: OpenAPI load specification for PUBLIC.FUGERIT.test_column_rename'
  description: 'Comments : '
  version: '1.0.0'
servers:
  - url: http://localhost:9080/fugerit-sample-web/jax-rs/testcolumnrename/load
    description: Local Development server
paths:
  /id/{id}:
    get:
      summary: Get PUBLIC.FUGERIT.test_column_rename by primary key
      parameters:
        - name: id
          in: path
          required: true
          description: Value of field ID
          schema:
            type: string
      responses:
        '200':
          description: Success response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestColumnRename'
  /all:
    get:
      summary: Get all PUBLIC.FUGERIT.test_column_rename
      responses:
        '200':
          description: Success response
          content:
            application/json:
              schema:
                  type: array
                  items: 
                    $ref: '#/components/schemas/TestColumnRename'
components: 
  schemas: 
    TestColumnRename: 
      properties:
        id:
          type: string
          example: ''
        renamedFieldOne:
          type: string
          example: ''
